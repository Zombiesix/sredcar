// 屏幕宽度字体自适应
const screenWidth = document.body.clientWidth
if (screenWidth >= 960) {
  document.documentElement.style.fontSize = '16px'
} else {
  document.documentElement.style.fontSize = '16px'
}
